program styling
      implicit none
      print *, 56789.66! I know this may feel weird you not putting your integers into quotes!but in fortran the print statement can
! print integers without any quotes at all but the only thing is that they are all right alligned also it can be used to print
! floats and doubles as well so if you run this particular code you wont get an error.
end program styling
